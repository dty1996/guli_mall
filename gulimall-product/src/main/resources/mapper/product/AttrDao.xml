<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.gulimall.product.dao.AttrDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.atguigu.gulimall.product.entity.AttrEntity" id="attrMap">
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="searchType" column="search_type"/>
        <result property="valueType" column="value_type"/>
        <result property="icon" column="icon"/>
        <result property="valueSelect" column="value_select"/>
        <result property="attrType" column="attr_type"/>
        <result property="enable" column="enable"/>
        <result property="catelogId" column="catelog_id"/>
        <result property="showDesc" column="show_desc"/>
    </resultMap>
    <select id="selectAttrsByCategoryId" resultType="com.atguigu.gulimall.product.entity.AttrEntity">
        SELECT pa.attr_id , pa.attr_name, pa.search_type, pa.value_type, pa.icon, pa.value_select, pa.enable, pa.catelog_id, pa.show_desc
        FROM pms_attr  pa
        <where>
            <if test="categoryId !=null and categoryId != ''">
                pa.catelogId = #{categoryId}
            </if>
        </where>
    </select>
    <select id="selectAttrVoPage" resultType="com.atguigu.gulimall.product.entity.AttrEntity">
        SELECT pa.attr_id , pa.attr_name, pa.search_type, pa.value_type, pa.icon, pa.value_select, pa.enable, pa.catelog_id, pa.show_desc,pa.attr_type,
                pag.attr_group_name AS group_name
        FROM pms_attr pa
        INNER JOIN pms_attr_attrgroup_relation  paar ON pa.attr_id = paar.attr_id
        INNER JOIN pms_attr_group pag ON pag.attr_group_id = paar.attr_group_id
        <where>
            pa.attr_type = #{attrType}
            <if test="catelogId != null and catelogId != '' ">
            AND     pa.catelogId = #{catelogId}
            </if>
        </where>
    </select>


</mapper>
