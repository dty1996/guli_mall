<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.gulimall.product.dao.AttrGroupDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.atguigu.gulimall.product.entity.AttrGroupEntity" id="attrGroupMap">
        <result property="attrGroupId" column="attr_group_id"/>
        <result property="attrGroupName" column="attr_group_name"/>
        <result property="sort" column="sort"/>
        <result property="descript" column="descript"/>
        <result property="icon" column="icon"/>
        <result property="catelogId" column="catelog_id"/>
    </resultMap>
    <select id="selectByCategoryId" resultType="com.atguigu.gulimall.product.entity.AttrGroupEntity">
        SELECT  ag.attr_group_id, ag.attr_group_name, ag.sort, ag.descript, ag.icon, ag.catelog_id
        FROM   pms_attr_group ag
        <where>
            ag.catelog_id = #{categoryId}
            <if test="key != null and key != ''">
                AND (  ag.attr_group_id = #{key} OR
                ag.attr_group_name LIKE CONCAT('%', #{key}, '%') )
            </if>
        </where>
    </select>
    <select id="selectAllPage" resultType="com.atguigu.gulimall.product.entity.AttrGroupEntity">
        SELECT  ag.attr_group_id, ag.attr_group_name, ag.sort, ag.descript, ag.icon, ag.catelog_id
        FROM   pms_attr_group ag
        <where>
            <if test="key != null and key != ''">
                 (ag.attr_group_id = #{key} OR
                ag.attr_group_name LIKE CONCAT('%', #{key}, '%') )
            </if>
        </where>
    </select>

</mapper>
